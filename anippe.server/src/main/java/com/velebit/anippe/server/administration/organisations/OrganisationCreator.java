package com.velebit.anippe.server.administration.organisations;

import com.velebit.anippe.shared.administration.organisations.OrganisationFormData;
import org.eclipse.scout.rt.platform.Bean;

@Bean
public class OrganisationCreator {

    public void create(OrganisationFormData formData) {
        //1. Create organisation
        //2. Create organisation settings
        //3. Create admin user
        //4. Create default role
        //5. Create default values (lead status, lead source)
    }
}
